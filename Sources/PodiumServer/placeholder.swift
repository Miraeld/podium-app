// PodiumServer
//
// Cross-platform (macOS + Linux) Hummingbird 2 HTTP server: REST API parity
// with the Node dashboard server, WebSocket broadcast hub, static serving of
// the bundled React dashboard (WebClient/dist).
//
// This file is a placeholder so the target builds before P2.1 lands the real
// server skeleton. Safe to delete once real sources exist.

import PodiumCore

public enum PodiumServerInfo {
    public static let placeholder = true
}
