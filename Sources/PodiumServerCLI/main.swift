// podium-server — headless daemon CLI.
//
// On Linux this IS the app: it serves the glassmorphism web dashboard over
// HTTP for browser use. Real argument parsing (swift-argument-parser) and
// server bootstrap land in P2.1; this is a placeholder entry point so the
// executable target builds end-to-end today.

import PodiumServer

print("podium-server: placeholder build (P2.1 will add the real CLI + server bootstrap)")
