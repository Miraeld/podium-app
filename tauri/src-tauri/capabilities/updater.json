{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater-window",
  "description": "Permissions for the custom updater window: listen for download-progress/error events emitted from Rust, and close itself on Later/dismiss.",
  "windows": ["updater"],
  "permissions": [
    "core:event:default",
    "core:window:allow-close"
  ]
}
