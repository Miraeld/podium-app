{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the dashboard (served from the localhost sidecar) to start native window dragging via data-tauri-drag-region.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:*", "http://127.0.0.1:*"]
  },
  "permissions": ["core:window:allow-start-dragging", "updater:default", "dialog:default"]
}
